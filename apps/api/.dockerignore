# The image is built from a `pnpm deploy --prod` output, so node_modules + dist +
# drizzle MUST be included. Exclude only dev/source cruft.
src
test
e2e
coverage
test-results
*.test.ts
tsconfig*.json
drizzle.config.ts
vitest.config.ts
playwright.config.ts
.turbo
