{
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "scripts/**", "projects/**", "astro.config.mjs", "tsconfig.json", "package.json"],
      "outputs": ["dist/**", "public/_sw-media/**"]
    },
    "test": {
      "dependsOn": ["^build", "build"],
      "inputs": ["src/**", "scripts/**", "test/**", "projects/**", "vitest.config.ts", "tsconfig.json"],
      "outputs": ["coverage/**"]
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "test/**", "tsconfig.json"]
    }
  }
}
