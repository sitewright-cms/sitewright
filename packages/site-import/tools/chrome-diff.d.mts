// Types for the chrome-diff shim — re-exported from the canonical TS source (src/fidelity/gate.ts).
export type { StyleEl, HoverState, ChromeEl, ChromePair, ChromeMatch, ChromeDiff, ChromeThresholds, ChromeScore, ChromeMeta, ChromeMetaScore } from '../src/fidelity/gate.js';
export { matchChrome, scoreChrome, scoreChromeMeta } from '../src/fidelity/gate.js';
